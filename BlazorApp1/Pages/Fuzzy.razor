@* @page "/fuzzy-matcher"

@using System

<h3>Fuzzy String Matching</h3>

<div class="form-group">
    <label for="string1">String 1:</label>
    <input type="text" class="form-control" id="string1" @bind="string1" />
</div>
<div class="form-group">
    <label for="string2">String 2:</label>
    <input type="text" class="form-control" id="string2" @bind="string2" />
</div>
<button class="btn btn-primary" @onclick="CalculateDistances">Calculate</button>

@if (calculated)
{
    <div class="mt-3">
        <p>Levenshtein Distance: @levenshteinDistance</p>
        <p>Jaro-Winkler Distance: @jaroWinklerDistance</p>
        <p>Match Percentage: @(matchPercentage * 100)%</p>
    </div>
}

@code {
    private string string1 = "";
    private string string2 = "";
    private int levenshteinDistance;
    private int jaroWinklerDistance;
    private double matchPercentage;
    private bool calculated = false;
    private FuzzyMatcher fuzzyMatcher = new FuzzyMatcher();

    private void CalculateDistances()
    {
        levenshteinDistance = fuzzyMatcher.LevenshteinDistance(string1, string2);
        jaroWinklerDistance = fuzzyMatcher.JaroWinklerDistance(string1, string2);
        matchPercentage = fuzzyMatcher.Match(string1, string2);
        calculated = true;
    }

    class FuzzyMatcher
    {
        public int LevenshteinDistance(string string1, string string2)
        {
            // Your existing implementation...
        }

        public int JaroWinklerDistance(string str1, string str2)
        {
            // Your existing implementation...
        }

        public int JaroDistance(string str1, string str2)
        {
            // Your existing implementation...
        }

        public double Match(string string1, string string2)
        {
            // Your existing implementation...
        }
    }
}
 *@